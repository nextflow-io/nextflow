---
title: timeline
description: Reference for the timeline config scope, which controls the generation of the execution timeline.
---

# `timeline`

The `timeline` scope controls the generation of the [execution timeline][timeline-report].

##### `timeline.enabled`

Create the execution timeline on workflow completion (default: `false`).

##### `timeline.file`

Timeline file name (default: `'timeline-<timestamp>.html'`).

Relative paths are resolved against the workflow output directory when it is defined, otherwise the launch directory.

##### `timeline.overwrite`

Overwrite any existing timeline file with the same name (default: `false`).

[timeline-report]: ../../reports#execution-timeline
