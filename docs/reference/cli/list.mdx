---
title: nextflow list
description: Reference for the nextflow list command. List all downloaded projects.
---

# `nextflow list`

List all downloaded projects.

## Usage

```console
$ nextflow list [options]
```

## Description

The `list` commands prints a list of the projects which are already downloaded into the global cache `~/.nextflow/assets`.

## Options

##### `-h, -help`

Print the command usage.

## Examples

List the downloaded pipelines:

```console
$ nextflow list

nextflow-io/hello
nextflow-hub/fastqc
```
