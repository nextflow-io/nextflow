config {
    // Per-provider nf-test suite for the unified `package` directive.
    // Each test is tagged with its provider; the CI matrix runs one tag per job
    // after provisioning the matching tool (see .github/workflows/package-providers.yml).
    testsDir "tests"
    workDir ".nf-test"
    configFile "package-providers.config"
    profile ""
}
