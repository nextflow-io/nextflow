nextflow_pipeline {

    name "pixi provider"
    script "../scripts/pixi.nf"
    tag "package-providers"
    tag "pixi"

    test("pixi resolves a package list and runs the task") {
        then {
            assert workflow.success
            assert workflow.stdout.any { it.contains('Hello pixi') }
        }
    }
}
