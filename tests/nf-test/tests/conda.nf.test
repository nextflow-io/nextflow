nextflow_pipeline {

    name "conda provider"
    script "../scripts/conda.nf"
    tag "package-providers"
    tag "conda"

    test("conda resolves a package list and runs the task") {
        then {
            assert workflow.success
            assert workflow.stdout.any { it.contains('Hello conda') }
        }
    }
}
