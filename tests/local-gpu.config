// Force 8-way scheduling regardless of the number of physical cores on the
// host, so that the 8 GPU tasks are guaranteed to run concurrently and each
// receive a distinct device id.
executor {
    cpus = 8
    queueSize = 8
}