set +e

#
# run normal mode
#
echo ''
$NXF_RUN | tee .stdout
[[ $? == 0 ]] || false
cmp .expected .stdout || false


#
# RESUME mode
#
echo ''
$NXF_RUN -resume | tee .stdout
[[ $? == 0 ]] || false
cmp .expected .stdout || false
