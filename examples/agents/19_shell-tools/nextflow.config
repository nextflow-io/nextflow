agent.runner = 'pi'

// `shell:bash` is served by the runner's own tools, so it exists only where the runner owns a
// container: `pi`. The langchain4j runner would have to execute the model's commands in the
// driver JVM, on the driver host, with no boundary at all.
docker.enabled = true
