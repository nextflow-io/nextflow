// Process config for fetchngs. It is kept apart from `nextflow.config`
// so that a meta-pipeline can include it as-is.
process {
    withName: 'SRATOOLS_FASTERQDUMP' {
        cpus   = 1
        memory = 1.GB
    }
}
