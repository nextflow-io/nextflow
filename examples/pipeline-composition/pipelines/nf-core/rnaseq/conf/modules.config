// Process config for rnaseq. It is kept apart from `nextflow.config`
// so that a meta-pipeline can include it as-is.
process {
    withName: 'STAR_ALIGN' {
        cpus   = 2
        memory = 2.GB
    }
    withName: 'SALMON_QUANT' {
        cpus   = 1
        memory = 1.GB
    }
}
