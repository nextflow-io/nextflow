machine:
    java:
        version: openjdk7
    services:
        - docker

checkout:
    post:
        - git submodule update --init
        
test:
    override:
        - ./gradlew assemble
        - ./gradlew test && ./integration-tests.sh :
            timeout: 1800
    